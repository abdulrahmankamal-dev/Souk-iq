<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Do not rewrite if directory or file exists
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    
    # Route everything else to index.php
    RewriteRule ^ index.php [L]
</IfModule>
